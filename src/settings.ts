/**
 * This is the name of the platform that users will use to register the plugin in the
 * Homebridge config.json
 */
export const PLATFORM_NAME = 'HomebridgeShortcutsButtons';

/**
 * This must match the name of your plugin as defined the package.json
 */
export const PLUGIN_NAME = 'homebridge-shortcuts-buttons';

/**
 * This plugin uses a single device/accessory just as a wrapper for the services
 */
export const DEVICE_SERIAL = '634EA867A81D59F1898';
